package gg.wildblood.client.gui

/**
 * Client-only screen classes (config screens, custom GUIs, HUD overlays).
 *
 * Menu/container classes that need to exist on the dedicated server too live in
 * [gg.wildblood.gui] instead.
 */