package gg.wildblood.util

/**
 * Shared, side-safe utility functions and constants.
 *
 * Keep this package free of any class that references client-only or
 * server-only Minecraft classes so it can be safely loaded on both sides.
 */